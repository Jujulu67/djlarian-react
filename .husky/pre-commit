# Vérification SQLite/PostgreSQL supprimée - les scripts de build s'en chargent automatiquement
# Le script ensure-postgresql-schema.sh s'exécute lors du build sur Vercel

# Exécuter lint-staged si configuré
npx lint-staged

