# Configuration pnpm pour le projet
# Résout le problème "Ignored build scripts" sur Vercel

# Autoriser les scripts de build pour les packages natifs
# Format pour pnpm: package-name=build-script-allowed
# Ces packages nécessitent l'exécution de leurs scripts postinstall

# Note: Avec packageManager défini sur pnpm@9.x dans package.json,
# ces restrictions ne s'appliquent pas. Ce fichier est une sécurité
# supplémentaire au cas où pnpm 10 serait utilisé.

# Activer l'installation automatique des peer dependencies
auto-install-peers=true

# Lever les dépendances au niveau racine (meilleure compatibilité Next.js)
shamefully-hoist=true
