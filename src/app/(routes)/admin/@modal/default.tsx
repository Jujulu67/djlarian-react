// Ce composant est rendu par défaut dans le slot @modal
// quand aucune route interceptée plus spécifique n'est active.
// On retourne null car on ne veut rien afficher par défaut.
export default function DefaultModal() {
  return null;
}
