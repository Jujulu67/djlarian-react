/**
 * Environment Configuration Module
 *
 * Provides typed, validated access to environment variables.
 *
 * Usage:
 *   import { serverEnv, isConfigured } from '@/lib/env';
 */

export { serverEnv, isConfigured, type ServerEnv } from './server';
